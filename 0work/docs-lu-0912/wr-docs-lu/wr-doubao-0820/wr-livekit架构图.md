

完整 UML 架构图--线程/协程驱动

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│  Layer 1: LiveKit SDK 业务层 (Kotlin/Java)                                   │
│                                                                             │
│  职责: 房间管理 · 信令协议 · 参与者模型 · Track 生命周期 · 发布/订阅逻辑      │
│  驱动: Kotlin 协程 — CoroutineScope(SupervisorJob + Dispatchers.Default)    │
│        disconnect() 时 cancel(SupervisorJob) → 级联清理全部业务协程          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌───────────────┐  creates   ┌──────────────────────────────────────────┐  │
│  │   LiveKit     │───────────>│                 Room                     │  │
│  │  (Singleton   │  owns 1..* │                                          │  │
│  │   Facade)     │            │  ┌─ 协程驱动核心 ─────────────────────┐  │  │
│  │               │            │  │ scope = CoroutineScope(SupervisorJob)│  │
│  │ +connect()    │            │  │                                      │  │
│  │ +disconnect() │            │  │  信令收发协程    状态机 Flow         │  │
│  │ +rooms:Map    │            │  │  Dispatchers.IO  <RoomState>         │  │
│  └───────────────┘            │  │                                      │  │
│                               │  │  发布协程        订阅协程             │  │
│  ★ LiveKit 只持有 Room,       │  │  suspend         suspend             │  │
│    不直接持有 SignalClient    │  │  publishTrack()  subscribe()         │  │
│    或 RTCEngine              │  │                                      │  │
│                               │  │  UI回调: .flowOn(Dispatchers.Main)   │  │
│                               │  └──────────────────────────────────────┘  │
│                               └────┬───────────────────┬───────────────────┘  │
│                                    │ owns 1            │ owns 1              │
│                                    ▼                   ▼                     │
│          ┌────────────────────┐    ┌──────────────────────────────────┐     │
│          │   SignalClient     │    │          RTCEngine               │     │
│          │   (信令客户端)     │    │       (WebRTC 引擎封装)          │     │
│          │                    │    │                                  │     │
│          │ +join()            │    │ +createPeerConnection()          │     │
│          │ +sendSignal()      │    │ +addTrack(track)                 │     │
│          │ +onSignalEvent()   │    │ +setLocalDescription(sdp)        │     │
│          │                    │    │ +setRemoteDescription(sdp)       │     │
│          └────────┬───────────┘    │ +addIceCandidate(candidate)     │     │
│                   │ owns 1         │ +onTrack(): RemoteTrack          │     │
│                   ▼                └──────────────┬───────────────────┘     │
│          ┌────────────────────┐                   │ owns 1                  │
│          │  WebSocket Client  │                   ▼                         │
│          │  (OkHttp, IO协程)  │      ┌──────────────────────────────────┐  │
│          │  收发 JSON 信令     │      │       PeerConnection             │  │
│          └────────────────────┘      │       (org.webrtc 层对象)        │  │
│                                      │                                  │  │
│                                      │ ← addTrack(LocalTrack 底层)     │  │
│                                      │ → onTrack() → 生成 RemoteTrack  │  │
│                                      │ ← setLocalDesc / setRemoteDesc  │  │
│                                      │ ← addIceCandidate               │  │
│                                      └──────────────────────────────────┘  │
│                                                                             │
│  ┌──────────────────────────┐         ┌──────────────────────────────┐      │
│  │   LocalParticipant       │         │   RemoteParticipant(s)       │      │
│  │   (本地参与者 / 发布者)   │         │   (远端参与者 1..* / 订阅者)  │      │
│  │                          │         │                              │      │
│  │ +publishTrack(track)     │         │ +onTrackPublished()          │      │
│  │ +unpublishTrack(sid)     │         │ +onTrackUnpublished()        │      │
│  └────────────┬─────────────┘         └──────────────┬───────────────┘      │
│               │ owns 0..*                            │ owns 0..*             │
│               ▼                                      ▼                      │
│  ┌──────────────────────────┐         ┌──────────────────────────────┐      │
│  │ LocalTrackPublication    │         │ RemoteTrackPublication       │      │
│  │ (发布元数据: SID / name  │         │ (订阅元数据: SID / name)     │      │
│  │  / muted / simulcast)    │         │                              │      │
│  └────────────┬─────────────┘         └──────────────┬───────────────┘      │
│               │ owns 1                               │ owns 1                │
│               ▼                                      ▼                      │
│  ┌──────────────────────────┐         ┌──────────────────────────────┐      │
│  │      LocalTrack          │         │      RemoteTrack             │      │
│  │   (LiveKit 业务层 Track) │         │   (LiveKit 业务层 Track)     │      │
│  │                          │         │                              │      │
│  │ +enable() / disable()    │         │ +attach(renderer)            │      │
│  │ +start() / stop()        │         │ +detach()                    │      │
│  └────────────┬─────────────┘         └──────────────┬───────────────┘      │
│               │ wraps (委托)                         │ wraps (委托)          │
└───────────────┼──────────────────────────────────────┼──────────────────────┘
                │                                      │
════════════════╪══════════════════════════════════════╪══════════════════════
                │  边界: wraps — LiveKit Track 持有     │
                │  org.webrtc.Track 引用, 不做任何     │
                │  音视频处理, 纯委托                  │
════════════════╪══════════════════════════════════════╪══════════════════════
                ▼                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  Layer 2: org.webrtc.* — Java JNI 桥接层 + Android 平台适配                  │
│                                                                             │
│  职责: ① JNI 映射: C++对象 → Java对象  ② 线程安全: ThreadUtils.invoke()     │
│        投递到 C++ 原生线程  ③ Android 适配: Camera2/SurfaceView/AudioRecord  │
│        ④ 引用计数: refCount 管理 C++ 对象释放                                │
│                                                                             │
│  ★ 此层不做任何编解码/网络/音频处理, 纯转发                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                    PeerConnection (Java)                              │  │
│  │                                                                        │  │
│  │  Java 方法                    JNI 投递              C++ 方法           │  │
│  │  ───────────                  ─────────             ──────────         │  │
│  │  +addTrack()         ──invoke(SignalingThread)──>  nativeAddTrack()   │  │
│  │  +removeTrack()      ──invoke(SignalingThread)──>  nativeRemoveTrack()│  │
│  │  +setLocalDescription()──invoke(SignalingThread)──> nativeSetLocalDesc│  │
│  │  +setRemoteDescription()─invoke(SignalingThread)──>nativeSetRemoteDesc│ │  │
│  │  +addIceCandidate()  ──invoke(SignalingThread)──>  nativeAddIce()     │  │
│  │  +createOffer()      ──invoke(SignalingThread)──>  nativeCreateOffer()│  │
│  │  +createAnswer()     ──invoke(SignalingThread)──>  nativeCreateAnswer│  │
│  │                                                                        │  │
│  │  Callbacks (C++ → Java, 经 SignalingThread 回调):                     │  │
│  │  ← onTrack()          ← onIceCandidate()       ← onConnectionChange() │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌────────────────────────┐              ┌────────────────────────────┐     │
│  │ org.webrtc.VideoTrack  │              │   org.webrtc.AudioTrack    │     │
│  │ (Java 映射对象, 不干活) │              │   (Java 映射对象, 不干活)  │     │
│  └──────────┬─────────────┘              └─────────────┬──────────────┘     │
═══════════════════════════════╪═════════════════════════════════════════════
                               │  JNI 边界
                               │
                               │  机制: Java native 方法声明 ↔ C++ JNI 函数
                               │  线程亲和性: 所有跨层调用必须通过
                               │  ThreadUtils.invoke() 投递到目标原生线程
                               │  否则 libwebrtc 抛出异常
                               │
═══════════════════════════════╪═════════════════════════════════════════════
                               ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  Layer 3: libwebrtc.so — C++ WebRTC 引擎本体                                 │
│                                                                             │
│  职责: 音视频编解码 · 网络传输 · 媒体处理 · 回声消除 · 拥塞控制 (所有重活)   │
│  驱动: 三大原生线程 (强制线程亲和性, 跨线程调用必须 invoke)                   │
│  跨平台: 同一套 C++ 代码编译到 Android/iOS/Windows/macOS/Linux               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │              ★ 三大原生线程 (Thread Affinity)                         │  │
│  │                                                                       │  │
│  │  ┌──────────────────┐  ┌──────────────────┐  ┌────────────────────┐  │  │
│  │  │ SignalingThread  │  │  WorkerThread    │  │  NetworkThread     │  │  │
│  │  │                  │  │                  │  │                    │  │  │
│  │  │ • SDP 创建/解析  │  │ • 编解码调度     │  │ • SRTP 加解密      │  │  │
│  │  │ • onTrack() 回调 │  │ • Jitter Buffer  │  │ • DTLS 握手        │  │  │
│  │  │ • ICE 候选回调   │  │ • AEC 回声消除   │  │ • ICE 连通性检查   │  │  │
│  │  │ • 状态变更通知   │  │ • NS 降噪        │  │ • RTP 打包/解包    │  │  │
│  │  │ • Offer/Answer   │  │ • AGC 自动增益   │  │ • RTCP 反馈        │  │  │
│  │  │   协商           │  │ • VAD 静音检测   │  │ • 带宽估计         │  │  │
│  │  │                  │  │ • 混音处理       │  │ • 拥塞控制(GCC)    │  │  │
│  │  │                  │  │ • 分辨率/帧率    │  │ • 包重传(NACK)     │  │  │
│  │  │                  │  │   适配           │  │ • FEC 前向纠错     │  │  │
│  │  └──────────────────┘  └──────────────────┘  └────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────────────┐  │
│  │   VideoEngine    │  │   AudioEngine    │  │   Transport / Network    │  │
│  │                  │  │                  │  │                          │  │
│  │ • VP8 / VP9 编码 │  │ • Opus 编解码    │  │ • ICE / STUN / TURN      │  │
│  │ • H264 编解码    │  │ • AEC3 回声消除  │  │ • DTLS-SRTP 加密通道     │  │
│  │ • 硬件加速(Media │  │ • NS 降噪        │  │ • RTP 打包 / 解包        │  │
│  │   Codec)         │  │ • AGC 增益控制   │  │ • RTCP 反馈 (RR/SR/NACK) │  │
│  │ • 分辨率降采样   │  │ • VAD 静音检测   │  │ • 带宽估计 (REMB/GCC)    │  │
│  │ • 帧率控制       │  │ • 混音           │  │ • 拥塞控制               │  │
│  │ • Simulcast      │  │ • NetEQ (抖动    │  │ • 包重传 (NACK)          │  │
│  │   多路编码       │  │   缓冲)          │  │ • FEC 前向纠错           │  │
│  │ • SVC 可伸缩编码 │  │                  │  │                          │  │
│  └──────────────────┘  └──────────────────┘  └──────────────────────────┘  │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  PeerConnection (C++)  — 真正的连接管理器                              │  │
│  │                                                                       │  │
│  │  持有:                                                                │  │
│  │  • RtpTransceiver (收发器, 管理一对 Sender/Receiver)                  │  │
│  │  • RtpSender / RtpReceiver (RTP 发送/接收器)                          │  │
│  │  • SessionDescription (SDP 本地/远端描述)                             │  │
│  │  • IceTransport (ICE 传输) / DtlsTransport (DTLS 加密)                │  │
│  │  • MediaStreamTrack (C++ 层 Track, 真正的媒体轨道)                    │  │
│  │  • DataChannel (可选, SCTP 数据通道)                                  │  │
│  │                                                                       │  │
│  │  ★ 所有音视频数据在此线程间流转:                                       │  │
│  │    采集→WorkerThread(编码)→NetworkThread(加密+发送)                   │  │
│  │    NetworkThread(接收+解密)→WorkerThread(解码+抖动缓冲)→渲染          │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

```



-> xxx类.xx函数（解码器）

完整架构图（含驱动模型）

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Application Layer (应用入口)                       │
│                                                                             │
│   ┌─────────────────────┐                                                   │
│   │      LiveKit        │  (Singleton / Facade 门面类)                      │
│   │   connect(url,token)│                                                   │
│   └──────────┬──────────┘                                                   │
└──────────────┼──────────────────────────────────────────────────────────────┘
               │ creates & owns (1..*)
               │   LiveKit 只持有 Room，不直接持有 SignalClient/RTCEngine
═══════════════╪═══════════════════════════════════════════════════════════════
               │         Room Scope (会话级生命周期边界)
               │    通话挂断 → 此线以下全部销毁 (SupervisorJob.cancel())
═══════════════╪═══════════════════════════════════════════════════════════════
               ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Room (核心会话调度者)                              │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  ★ 协程驱动核心 (CoroutineScope)                                       │  │
│  │  scope = CoroutineScope(SupervisorJob() + Dispatchers.Default)        │  │
│  │                                                                       │  │
│  │  ┌─────────────┐  ┌──────────────┐  ┌─────────────┐  ┌────────────┐  │  │
│  │  │ 信令收发协程 │  │ 状态机 Flow  │  │ 发布协程    │  │ 订阅协程   │  │  │
│  │  │ Dispatchers │  │<RoomState>   │  │ suspend    │  │ suspend   │  │  │
│  │  │ .IO         │  │ Connecting→  │  │ publishTrk │  │ subscribe │  │  │
│  │  │             │  │ Connected→   │  │ ()         │  │ ()        │  │  │
│  │  │             │  │ Reconnecting │  │            │  │           │  │  │
│  │  └─────────────┘  └──────────────┘  └─────────────┘  └────────────┘  │  │
│  │                                                                       │  │
│  │  UI 回调: .flowOn(Dispatchers.Main)  ←  状态变化通知 UI 层             │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│        │ drives (协程调度)                    │ drives (协程调度)            │
│        ▼                                      ▼                             │
│  ┌────────────────────┐   SDP/ICE    ┌──────────────────────────┐           │
│  │   SignalClient     │ <──────────> │       RTCEngine          │           │
│  │   (信令客户端)      │  (信令驱动    │     (WebRTC 引擎封装)    │           │
│  │                    │   媒体协商)   │                          │           │
│  │  +sendSignal()     │              │  +createPeerConnection() │           │
│  │  +onSignalEvent()  │              │  +addTrack()             │           │
│  │  +join()           │              │  +setLocalDescription()  │           │
│  └─────────┬──────────┘              │  +setRemoteDescription() │           │
│            │ owns 1                  │  +onTrack():RemoteTrack  │           │
│            ▼                         └─────────────┬────────────┘           │
│  ┌────────────────────┐                            │ owns 1                 │
│  │  WebSocket Client  │                            ▼                       │
│  │  (OkHttp, IO协程)  │               ┌──────────────────────────┐         │
│  │  收发 JSON 信令     │               │    PeerConnection        │         │
│  └────────────────────┘               │   (org.webrtc.PeerConn)  │         │
│                                       │                          │         │
│                                       │  ← addTrack(LocalTrack) │         │
│                                       │  → onTrack(RemoteTrack) │         │
│                                       │  ← setLocalDesc(SDP)    │         │
│                                       │  ← setRemoteDesc(SDP)   │         │
│                                       │  ← addIceCandidate()    │         │
│                                       └─────────────┬────────────┘         │
│                                                     │                      │
│  ┌─────────────────────────┐      ┌─────────────────┴──────────────┐       │
│  │   LocalParticipant      │      │    RemoteParticipant(s)        │       │
│  │   (本地参与者/发布者)    │      │    (远端参与者 1..* /订阅者)    │       │
│  │                         │      │                                │       │
│  │  +publishTrack()        │      │  +onTrackPublished()           │       │
│  │  +unpublishTrack()      │      │  +onTrackUnpublished()         │       │
│  └───────────┬─────────────┘      └──────────────┬─────────────────┘       │
│              │ owns 0..*                         │ owns 0..*                │
│              ▼                                   ▼                        │
│  ┌─────────────────────────┐      ┌──────────────────────────────────┐    │
│  │ LocalTrackPublication   │      │ RemoteTrackPublication           │    │
│  │ (发布元数据: SID/name)   │      │ (订阅元数据: SID/name)           │    │
│  └───────────┬─────────────┘      └──────────────┬───────────────────┘    │
│              │ owns 1                            │ owns 1                  │
│              ▼                                   ▼                       │
│  ┌─────────────────────────┐      ┌──────────────────────────────────┐    │
│  │      LocalTrack         │      │         RemoteTrack              │    │
│  │   (LiveKit 业务层)      │      │      (LiveKit 业务层)            │    │
│  │                         │      │                                  │    │
│  │  +enable()/disable()    │      │  +attach(renderer)              │    │
│  │  +start()/stop()        │      │  +detach()                      │    │
│  └───────────┬─────────────┘      └──────────────┬───────────────────┘    │
└──────────────┼────────────────────────────────────┼────────────────────────┘
               │ wraps                              │ wraps
═══════════════╪════════════════════════════════════╪══════════════════════════
               │    技术栈边界 (Kotlin/Java 层)      │
═══════════════╪════════════════════════════════════╪══════════════════════════
               ▼                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                org.webrtc.* (Google WebRTC Java 层)                          │
│                                                                             │
│  ┌───────────────────────┐                  ┌───────────────────────────┐   │
│  │ org.webrtc.VideoTrack │                  │  org.webrtc.AudioTrack    │   │
│  └───────────┬───────────┘                  └─────────────┬─────────────┘   │
│              │ feeds from (本地采集)          feeds from (本地采集)          │
│              ▼                                             ▼                │
│  ┌───────────────────────┐                  ┌───────────────────────────┐   │
│  │ VideoCapturer / Source│                  │ JavaAudioDeviceModule     │   │
│  │ (Camera2/ScreenCapt)  │                  │ (麦克风/AEC/降噪/混音)    │   │
│  └───────────┬───────────┘                  └─────────────┬─────────────┘   │
│              │                                            │                 │
│              │              ┌──────────────┐              │                 │
│              └──────────────│ PeerConnect. │──────────────┘                 │
│                             │  addTrack()  │  (将 Track 加入传输通道)        │
│                             └──────┬───────┘                               │
│                                    │                                       │
│              ┌─────────────────────┼─────────────────────┐                 │
│              │ onTrack() 回调生成   │                     │                 │
│              ▼                     ▼                     ▼                 │
│  ┌───────────────────┐  ┌───────────────────┐  ┌───────────────────┐       │
│  │SurfaceViewRenderer│  │   AudioSink       │  │  Remote Track     │       │
│  │ (视频渲染到屏幕)   │  │  (音频播放到扬声器) │  │  (接收端)         │       │
│  └───────────────────┘  └───────────────────┘  └───────────────────┘       │
└─────────────────────────────────┬───────────────────────────────────────────┘
                                  │
══════════════════════════════════╪══════════════════════════════════════════
                                  │  JNI 边界 (Java Native Interface)
                                  │  ★ 线程亲和性: Java 调用必须通过
                                  │    ThreadUtils.invoke() 投递到原生线程
══════════════════════════════════╪══════════════════════════════════════════
                                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                   libwebrtc.so (C++ WebRTC 底层引擎)                         │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  ★ 三大原生线程 (强制线程亲和性, ThreadUtils.invoke)                    │  │
│  │                                                                       │  │
│  │  ┌──────────────────┐  ┌──────────────────┐  ┌────────────────────┐  │  │
│  │  │ SignalingThread  │  │  WorkerThread    │  │  NetworkThread     │  │  │
│  │  │                  │  │                  │  │                    │  │  │
│  │  │ • SDP 协商回调   │  │ • 编解码(VP8/H264│  │ • SRTP 加解密      │  │  │
│  │  │ • onTrack() 触发 │  │   /Opus)         │  │ • DTLS 握手        │  │  │
│  │  │ • ICE 候选回调   │  │ • 媒体处理       │  │ • ICE 连通性检查   │  │  │
│  │  │ • 状态变更通知   │  │ • Jitter Buffer │  │ • RTP/RTCP 收发    │  │  │
│  │  │                  │  │ • 回声消除(AEC)  │  │ • 带宽估计         │  │  │
│  │  └──────────────────┘  └──────────────────┘  └────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────────┐      │
│  │ VideoEngine  │  │ AudioEngine  │  │     Transport / Network      │      │
│  │ (视频引擎)   │  │ (音频引擎)   │  │     (传输/网络层)             │      │
│  └──────────────┘  └──────────────┘  └──────────────────────────────┘      │
└─────────────────────────────────────────────────────────────────────────────┘


```